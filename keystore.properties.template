# Release Keystore 설정 템플릿
# 이 파일을 keystore.properties로 복사하고 실제 값을 입력하세요.
# keystore.properties는 .gitignore에 추가하여 Git에 커밋하지 마세요.

# Keystore 파일 경로 (프로젝트 루트 기준 상대 경로 또는 절대 경로)
storeFile=keystore/release.keystore

# Keystore 비밀번호
storePassword=your_keystore_password

# Key alias
keyAlias=tikka_timer_release

# Key 비밀번호
keyPassword=your_key_password
